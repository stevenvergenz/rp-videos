{
  "name": "video-manager",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "main": "built/server.js",
  "author": "Steven Vergenz",
  "license": "MIT",
  "engines": {
    "node": ">=8.12.0",
    "npm": ">=6.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "lint": "tslint -p ./tsconfig.json src/**/*.ts",
    "start": "node .",
    "debug": "node --nolazy --inspect-brk=9229 ."
  },
  "devDependencies": {
    "@types/redis": "^2.8.12",
    "tslint": "5.11.0",
    "typescript": "3.0.3"
  },
  "dependencies": {
    "@microsoft/mixed-reality-extension-altspacevr-extras": "^0.13.1",
    "@microsoft/mixed-reality-extension-sdk": "^0.13.1",
    "@types/node": "^10.3.1",
    "googleapis": "^39.0.0"
  }
}
